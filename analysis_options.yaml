include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Promover a error cosas que no deberían llegar a main
    missing_required_param: error
    missing_return: error
    todo: ignore
    # Warnings explícitos
    invalid_annotation_target: warning
    unused_import: warning
    unused_local_variable: warning
    dead_code: warning
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.config.dart"
    - "lib/firebase_options.dart"
    - "build/**"

linter:
  rules:
    # Estilo
    prefer_single_quotes: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_locals: true
    prefer_final_in_for_each: true
    sort_child_properties_last: true
    use_key_in_widget_constructors: true

    # Seguridad / corrección
    avoid_print: false  # TODO: re-enable after cleanup
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    throw_in_finally: true
    unawaited_futures: false  # TODO: re-enable after cleanup

    # Mantenibilidad
    always_declare_return_types: true
    avoid_empty_else: true
    avoid_relative_lib_imports: true
    avoid_types_as_parameter_names: true
    avoid_unnecessary_containers: true
    avoid_void_async: true
    empty_catches: true
    no_duplicate_case_values: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    unnecessary_null_checks: true
    unnecessary_this: true
